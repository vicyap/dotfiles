@AGENTS.md

# Claude Code

When suggesting tool installs or aliases, check `CHEATSHEET.md`, `nix/home/features/packages.nix`, `platform/macos/Brewfile`, `platform/linux/packages.txt`, and `packages/mise/.config/mise/config.toml` first — most modern CLIs are already managed (the CLI tool set now lives in Nix home-manager). Honor the omakase curation: cohesion over options.
