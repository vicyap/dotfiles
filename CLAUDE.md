@AGENTS.md

# Claude Code

When suggesting tool installs or aliases, check `CHEATSHEET.md`, `platform/macos/Brewfile`, `platform/linux/packages.txt`, and `packages/mise/.config/mise/config.toml` first — most modern CLIs are already managed. Honor the omakase curation: cohesion over options.
