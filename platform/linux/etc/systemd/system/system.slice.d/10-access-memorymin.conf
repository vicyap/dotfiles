# memory.min protection only takes effect if every ancestor cgroup allocates
# it too (systemd.resource-control(5)). This covers the MemoryMin=128M claims
# of ssh.service and tailscaled.service; unclaimed remainder is shared.
[Slice]
MemoryMin=256M
